import { calcHouseholdProjection } from '../src/engines/householdEngine.js';

const base = {
  householdMode: 'joint',
  currentAge: 42,
  retireAge: 67,
  stateAge: 67,
  valuationDate: '2026-03-21',
  firstYearMonths: 6.3,
  pot: 200000,
  salary: 90000,
  empPct: 8,
  erPct: 10,
  statePension: 12000,
  otherIncome: 0,
  salaryGrowth: 3,
  returnNom: 5,
  inflation: 2.5,
  drawdown: 4,
  endAge: 90,
  allowance: 12570,
  basicTax: 20,
  higherTax: 40,
  higherThreshold: 50270,
  tflsPct: 25,
  tflsCap: 268275,
  feePct: 0.4,
  dcPensions: [{ id: 'old-a', name: 'Old pot', currentValue: 50000, feePct: 0.7, priority: 60 }],
  dbPensions: [{ annualIncome: 5000, startAge: 67, increaseType: 'cpi' }],
  contribEvents: [],
  lumpSumEvents: [],
  partnerCurrentAge: 40,
  partnerRetireAge: 65,
  partnerStateAge: 67,
  partnerValuationDate: '2026-03-21',
  partnerFirstYearMonths: 4.2,
  partnerPot: 100000,
  partnerSalary: 40000,
  partnerEmpPct: 5,
  partnerErPct: 5,
  partnerStatePension: 11000,
  partnerOtherIncome: 0,
  partnerDcPensions: [{ id: 'partner-old', name: 'Partner old pot', currentValue: 30000, feePct: 0.5, priority: 40 }],
  partnerDbPensions: [{ annualIncome: 4000, startAge: 65, increaseType: 'fixed', escalationPct: 2 }],
  partnerContribEvents: [],
  partnerLumpSumEvents: [],
};

const result = calcHouseholdProjection(base);
const bothRetired = result.years.find((y) => y.age === result.firstBothRetiredAge);
console.log({
  years: result.years.length,
  firstBothRetiredAge: result.firstBothRetiredAge,
  bothRetired,
  combinedPotAtBothRetired: result.combinedPotAtBothRetired,
});
